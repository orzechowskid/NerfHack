diff --git a/src/fountain.c b/src/fountain.c
index 616d42b4..f3664768 100644
--- a/src/fountain.c
+++ b/src/fountain.c
@@ -67,7 +67,7 @@ dowaterdemon()
 
             /* Give those on low levels a (slightly) better chance of survival
              */
-            if (rnd(100) > (80 + level_difficulty())) {
+            if (rnd(110) > (80 + level_difficulty())) {
                 pline("Grateful for %s release, %s grants you a wish!",
                       mhis(mtmp), mhe(mtmp));
                 /* give a wish and discard the monster (mtmp set to null) */
@@ -224,7 +224,7 @@ drinkfountain()
 {
     /* What happens when you drink from a fountain? */
     register boolean mgkftn = (levl[u.ux][u.uy].blessedftn == 1);
-    register int fate = rnd(30);
+    register int fate = rnd(35);
 
     if (Levitation) {
         floating_above("fountain");
@@ -301,11 +301,12 @@ drinkfountain()
             morehungry(rn1(20, 11));
             exercise(A_CON, FALSE);
             for (obj = invent; obj; obj = obj->nobj)
-                if (!rn2(5))
+                if (!rn2(7))
                     curse(obj);
             break;
         }
         case 25: /* See invisible */
+        case 31:
             if (Blind) {
                 if (Invisible) {
                     You("feel transparent.");
@@ -322,6 +323,7 @@ drinkfountain()
             exercise(A_WIS, TRUE);
             break;
         case 26: /* See Monsters */
+        case 32:
             (void) monster_detect((struct obj *) 0, 0);
             exercise(A_WIS, TRUE);
             break;
diff --git a/src/mklev.c b/src/mklev.c
index 5f6afff4..bfd02ffa 100644
--- a/src/mklev.c
+++ b/src/mklev.c
@@ -823,7 +823,7 @@ skip0:
             mkfount(0, croom);
         if (!rn2(60))
             mksink(croom);
-        if (!rn2(60))
+        if (!rn2(50))
             mkaltar(croom);
         x = 80 - (depth(&u.uz) * 2);
         if (x < 2)
@@ -1555,7 +1555,7 @@ struct mkroom *croom;
     /* Put a fountain at m.x, m.y */
     levl[m.x][m.y].typ = FOUNTAIN;
     /* Is it a "blessed" fountain? (affects drinking from fountain) */
-    if (!rn2(7))
+    if (!rn2(5))
         levl[m.x][m.y].blessedftn = 1;
 
     level.flags.nfountains++;
diff --git a/src/mkobj.c b/src/mkobj.c
index 1724cded..33b015b6 100644
--- a/src/mkobj.c
+++ b/src/mkobj.c
@@ -28,23 +28,23 @@ static const struct icp mkobjprobs[] = { { 10, WEAPON_CLASS },
                                          { 10, ARMOR_CLASS },
                                          { 20, FOOD_CLASS },
                                          { 8, TOOL_CLASS },
-                                         { 8, GEM_CLASS },
+                                         { 6, GEM_CLASS },
                                          { 16, POTION_CLASS },
                                          { 16, SCROLL_CLASS },
                                          { 4, SPBOOK_CLASS },
                                          { 4, WAND_CLASS },
                                          { 3, RING_CLASS },
-                                         { 1, AMULET_CLASS } };
+                                         { 3, AMULET_CLASS } };
 
-static const struct icp boxiprobs[] = { { 18, GEM_CLASS },
+static const struct icp boxiprobs[] = { { 8, GEM_CLASS },
                                         { 15, FOOD_CLASS },
                                         { 18, POTION_CLASS },
-                                        { 18, SCROLL_CLASS },
-                                        { 12, SPBOOK_CLASS },
+                                        { 20, SCROLL_CLASS },
+                                        { 14, SPBOOK_CLASS },
                                         { 7, COIN_CLASS },
-                                        { 6, WAND_CLASS },
-                                        { 5, RING_CLASS },
-                                        { 1, AMULET_CLASS } };
+                                        { 8, WAND_CLASS },
+                                        { 7, RING_CLASS },
+                                        { 3, AMULET_CLASS } };
 
 static const struct icp rogueprobs[] = { { 12, WEAPON_CLASS },
                                          { 12, ARMOR_CLASS },
@@ -768,7 +768,7 @@ boolean artif;
         case WEAPON_CLASS:
             otmp->quan = is_multigen(otmp) ? (long) rn1(6, 6) : 1L;
             if (!rn2(11)) {
-                otmp->spe = rne(3);
+                otmp->spe = rne(3) + 1;
                 otmp->blessed = rn2(2);
             } else if (!rn2(10)) {
                 curse(otmp);
@@ -963,7 +963,7 @@ boolean artif;
                 otmp->spe = -rne(3);
             } else if (!rn2(10)) {
                 otmp->blessed = rn2(2);
-                otmp->spe = rne(3);
+                otmp->spe = rne(3) + 2;
             } else
                 blessorcurse(otmp, 10);
             if (artif && !rn2(40))
@@ -1336,7 +1336,7 @@ register int chance;
         return;
 
     if (!rn2(chance)) {
-        if (!rn2(2)) {
+        if (!rn2(3)) {
             curse(otmp);
         } else {
             bless(otmp);
diff --git a/src/pickup.c b/src/pickup.c
index fae1e2f0..352d6549 100644
--- a/src/pickup.c
+++ b/src/pickup.c
@@ -2063,7 +2063,7 @@ int held;
 
         for (curr = container->cobj; curr; curr = otmp) {
             otmp = curr->nobj;
-            if (!rn2(13)) {
+            if (!rn2(20)) {
                 obj_extract_self(curr);
                 loss += mbag_item_gone(held, curr);
             }
diff --git a/src/pray.c b/src/pray.c
index d0cb794a..b7b27074 100644
--- a/src/pray.c
+++ b/src/pray.c
@@ -1716,7 +1716,7 @@ dosacrifice()
             /* The player can gain an artifact */
             /* The chance goes down as the number of artifacts goes up */
             if (u.ulevel > 2 && u.uluck >= 0
-                && !rn2(10 + (2 * u.ugifts * nartifacts))) {
+                && !rn2(10 + (2 * (u.ugifts + nartifacts)))) {
                 otmp = mk_artifact((struct obj *) 0, a_align(u.ux, u.uy));
                 if (otmp) {
                     if (otmp->spe < 0)
diff --git a/src/restore.c b/src/restore.c
index b967e146..993596ef 100644
--- a/src/restore.c
+++ b/src/restore.c
@@ -334,7 +334,6 @@ struct monst *mtmp;
     int buflen;
 
     mread(fd, (genericptr_t) mtmp, sizeof(struct monst));
-
     /* next monster pointer is invalid */
     mtmp->nmon = (struct monst *) 0;
     /* non-null mextra needs to be reconstructed */
diff --git a/src/timeout.c b/src/timeout.c
index a469eb11..e7767f13 100644
--- a/src/timeout.c
+++ b/src/timeout.c
@@ -369,7 +369,7 @@ nh_timeout()
     struct kinfo *kptr;
     int sleeptime;
     int m_idx;
-    int baseluck = (flags.moonphase == FULL_MOON) ? 1 : 0;
+    int baseluck = (flags.moonphase == FULL_MOON) ? 2 : 1;
 
     if (flags.friday13)
         baseluck -= 1;
